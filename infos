 <Button variant="outlined" size="sm" onClick={() => DocumentService.downloadOrViewDocument(doc.id, true)}>
                        <Download className="w-4 h-4"/>
                        Télécharger
                      </Button>
